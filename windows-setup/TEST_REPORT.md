# Quest Home Switcher 1.1 verification report

Date: 2026-07-14

This report covers the final 1.1 hotfix source, signed Android APK, one-file Windows setup, secure release updater, and a connected Meta Quest test. The running Shizuku server was not stopped, restarted, upgraded, paired, or reconfigured during the update and activation checks.

## Final artifacts

| Artifact | Version | SHA-256 |
| --- | --- | --- |
| `Quest-Home-Switcher-v1.1.apk` | Android versionCode 14 / versionName 1.1 | `68FA680A7315172197951E7892D3CD306F8BE8A2E8FD8074D94D7B9B5D163DBA` |
| `Quest-Home-Switcher-Setup-v1.1.exe` | FileVersion 1.1.0.0 | `16EE7BEE4330860AD129471E009C3883EB6B92DCAC6FAC14F09DF919BDBA5DD8` |

- APK package: `io.github.nikitat21.questhomeswitcher`
- APK certificate SHA-256: `85569394c59b355e850c540ac8b3247e27fbde16235ce20e95bcead337d93f75`
- The certificate exactly matches the public 1.0 APK, so Android accepts an in-place update.
- The setup's embedded APK is byte-for-byte identical to the standalone release APK.
- The setup launcher verifies the embedded APK hash before starting PowerShell.
- The EXE is not Authenticode-signed; Windows may show **Unknown publisher** or a SmartScreen reputation warning.

## Android checks

- 36 debug unit tests passed with zero failures or skips.
- The complete release unit-test suite passed.
- Debug and release Android Lint passed.
- Release assembly, zip alignment, APK signing, and `apksigner` verification passed.
- Package ID, versionCode, versionName, and permanent signing certificate were checked after signing.
- Activation tests cover delayed package publication, all supported `pm path` and `cmd package path` variants, multiple APK paths, normalized exact scene hashes, bounded polling, large-APK hash caching, rollback, failed rollback preservation, and post-reload integrity checks.
- ViewModel tests cover serialized Apply/Restart/Debug operations, rejection of overlapping actions, cancellation cleanup, refresh-worker recovery, and non-deadlocking post-activation refresh.
- Active-Home tests cover all package-path fallbacks and exact normalized scene-hash matching.
- Official-Home fallback artwork is generated by the app's own Compose canvas code. No Meta preview image, Home APK, `scene.zip`, or other proprietary Home content is included.

## Windows setup and updater checks

- PowerShell parsing and WPF XAML loading passed.
- The isolated one-file EXE self-test passed with no sibling APK and left no temporary payload or updater files behind.
- Home APK validation, naming review, duplicate handling, collision-safe import, ADB stderr handling, remote size verification, and remote SHA-256 verification passed.
- `IMPORT HOME APKS` and `UPDATE / OPEN SWITCHER` were proven not to call Shizuku setup or pairing logic.
- Signature-migration tests prove that an incompatible installed test signature is removed only after explicit user approval; rejection leaves the installed app untouched.
- The secure updater accepts only this repository's final, non-draft, non-prerelease releases with exact tag-derived asset names and URLs.
- GitHub-provided asset SHA-256 digests and sizes are mandatory and verified before use.
- Newer releases require exactly one hidden positive Android versionCode marker; missing, malformed, duplicate, or non-incrementing markers fail safely to the embedded APK.
- A downloaded setup is opened only when its FileVersion matches the release tag, preventing stale self-update loops.
- Offline, malformed, or unverifiable release metadata keeps the embedded signed APK selected.
- A remotely downloaded APK with a different Android signing certificate fails closed and does not trigger an uninstall.

Self-test marker:

```text
SELF_TEST_OK_XAML_OK_PAYLOAD_OK_STATE_MACHINE_OK_HOME_IMPORT_OK_ADB_PUSH_OK_IMPORT_UI_OK_COOKED_PICKER_OK_PROFESSIONAL_NAMING_OK_SECURE_RELEASE_UPDATE_OK_SIGNATURE_MIGRATION_OK_FAST_MODES_NO_SHIZUKU_OK
```

## Connected Quest verification

- The final 1.1 APK installed successfully over the existing app.
- Shizuku API permission remained granted across the update.
- The app launched cold and showed `Shizuku connected`, `Ready`, 16 detected Homes, and the correct active Home.
- A real Apply operation replaced the carrier APK path and the installed decompressed `assets/scene.zip` SHA-256 exactly matched the selected Home afterward.
- Two immediate taps on Apply did not start overlapping Home operations.
- After activation the app returned to `Ready` and displayed the correct active Home.
- The package-manager fallback commands and Android `stat`, `unzip`, and SHA-256 tools used by 1.1 were also checked directly on the Quest.

## Upgrade note

Setup 1.0 cannot retroactively acquire the updater code. Existing users must download setup 1.1 once. Setup 1.1 then provides the verified update path for later final releases.
