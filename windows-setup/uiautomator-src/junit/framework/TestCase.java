package junit.framework;

// Compile-only stub. Android's UiAutomator runtime supplies the real class.
public class TestCase {
    protected static void fail(String message) {
        throw new AssertionError(message);
    }
}
